Upload these two replacement files to the ROOT of the Newton-Wedding repository:

- styles.css
- script.js

Allow GitHub to replace the existing files, then commit the changes.
The fix makes the opening invitation scrollable on shorter screens and keeps the wax-seal button accessible.
