const opening=document.getElementById("opening"),openButton=document.getElementById("openInvitation"),body=document.body,loader=document.getElementById("cinematicLoader");function finishLoader(){if(!loader)return;loader.classList.add("is-finished");body.classList.remove("loading");setTimeout(()=>loader.style.display="none",950)}window.addEventListener("load",()=>setTimeout(finishLoader,window.matchMedia("(prefers-reduced-motion: reduce)").matches?200:6500));function openSite(){if(!opening)return;opening.classList.add("is-open");body.classList.remove("locked");document.documentElement.style.overflow="";sessionStorage.setItem("newtonInvitationOpened","true");setTimeout(()=>opening.style.display="none",950)}if(openButton){openButton.addEventListener("click",openSite);openButton.addEventListener("touchend",e=>{e.preventDefault();openSite()},{passive:false})}if(opening&&sessionStorage.getItem("newtonInvitationOpened")==="true"){opening.style.display="none";body.classList.remove("locked")}const header=document.querySelector(".site-header");window.addEventListener("scroll",()=>{if(header)header.classList.toggle("scrolled",scrollY>40)},{passive:true});const menuButton=document.getElementById("menuButton"),siteNav=document.getElementById("siteNav");if(menuButton&&siteNav){menuButton.addEventListener("click",()=>{const o=siteNav.classList.toggle("open");menuButton.setAttribute("aria-expanded",String(o))});siteNav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{siteNav.classList.remove("open");menuButton.setAttribute("aria-expanded","false")}))}const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("in-view");observer.unobserve(e.target)}}),{threshold:.14});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));const weddingDate=new Date("2027-09-25T14:00:00-04:00"),countdown=document.getElementById("countdown");function updateCountdown(){if(!countdown)return;const d=weddingDate-new Date;if(d<=0){countdown.innerHTML="<div><strong>Today</strong><span>We say I do</span></div>";return}const days=Math.floor(d/86400000),hours=Math.floor(d%86400000/3600000),minutes=Math.floor(d%3600000/60000),seconds=Math.floor(d%60000/1000);countdown.innerHTML=`<div><strong>${days}</strong><span>Days</span></div><div><strong>${hours}</strong><span>Hours</span></div><div><strong>${minutes}</strong><span>Minutes</span></div><div><strong>${seconds}</strong><span>Seconds</span></div>`}updateCountdown();setInterval(updateCountdown,1000);
